# =============================================================================
# ROS2 / colcon build artifacts
# =============================================================================
# colcon이 워크스페이스 루트에 생성하는 디렉터리 (재빌드 가능, 절대 커밋 금지)
build/
install/
log/

# ament/colcon 부산물
**/COLCON_IGNORE
**/AMENT_IGNORE

# =============================================================================
# Python build artifacts (ros2 pkg가 setup.py 기반이라 src/ 안에도 생김)
# =============================================================================
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# =============================================================================
# C++ build artifacts (cmake 패키지가 src 안에서 빌드되는 경우 대비)
# =============================================================================
*.o
*.so
*.a
*.lo
*.la
CMakeCache.txt
CMakeFiles/
cmake_install.cmake

# =============================================================================
# ROS2 bag 파일 (용량 크고 실험별로 다름)
# =============================================================================
rosbag2_*/
*.bag/
*.db3
*.mcap

# =============================================================================
# 데이터셋 / 학습 결과물 (대용량 바이너리)
# =============================================================================
Collected_Datasets/
*.pt
*.pth
*.onnx
*.engine
*.weights

# =============================================================================
# 에디터 / OS
# =============================================================================
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db

# =============================================================================
# 환경별 로컬 설정 (필요 시 .example 파일로 템플릿 커밋)
# =============================================================================
*.local.yaml
*.local.xml
.env
.env.local
